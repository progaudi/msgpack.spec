variables:
  sln: msgpack.spec.sln
  tests: tests/msgpack.spec.tests/msgpack.spec.tests.csproj

resources:
  repositories:
    - repository: templates
      type: github
      name: progaudi/azure-pipelines
      endpoint: progaudi

jobs:
- template: library/nuget.yml@templates
